llama-cpp-python==0.2.6
requests==2.31.0
yfinance==0.2.36
beautifulsoup4==4.12.3
newspaper3k==0.2.8
lxml_html_clean==0.4.5
python-dotenv==1.0.1
fastapi==0.109.1
starlette>=0.35.0,<0.36.0
uvicorn==0.27.1
pydantic==2.6.1
tenacity==8.2.3
aiohttp==3.9.3 

# Test dependencies. The README tells contributors to run pytest, so these
# belong here rather than being assumed present in the ambient environment.
pytest==8.4.2
pytest-asyncio==1.2.0
# starlette 0.35.x TestClient passes app= to httpx.Client, removed in httpx 0.28
httpx>=0.27,<0.28
